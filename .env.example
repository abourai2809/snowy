# Frontend runtime config. Copy to .env.local for local development.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase CLI/project metadata. Keep real values in .env.local or the CLI config,
# never in Git.
SUPABASE_PROJECT_REF=
SUPABASE_DB_URL=
